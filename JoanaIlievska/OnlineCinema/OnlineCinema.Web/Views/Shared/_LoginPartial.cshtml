@using Microsoft.AspNetCore.Identity
@using OnlineCinema.Domain.Identity;
@using System.Text.RegularExpressions;
@inject SignInManager<OnlineCinemaApplicationUser> SignInManager
@inject UserManager<OnlineCinemaApplicationUser> UserManager


<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link text-white" asp-area="Identity" style="font-family: 'Times New Roman', serif;" asp-page="/Account/Manage/Index" title="Manage">Добредојде!</a>
        </li>
        <li class="nav-item">
            @*<form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
        <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>*@
            <a class="nav-link text-white" style="font-family: 'Times New Roman', serif;"  asp-controller="Account" asp-action="Logout">Одјави се</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white" style="font-family: 'Times New Roman', serif;" asp-controller="User" asp-action="Index">Регистрирај корисници</a>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-white" style="font-family: 'Times New Roman', serif;"  asp-controller="Account" asp-action="Register">Регистирација</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white" style="font-family: 'Times New Roman', serif;" asp-controller="Account" asp-action="Login">Најава</a>
        </li>
    }
</ul>